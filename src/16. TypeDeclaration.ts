console.log(
    'logged in use',
    //process.env.USER

    //uncomment the above line
    //install definitely types from npm
    //npm i @types/node
    //process is a node type we use to check env variables
    //using this npm package we don't need to create our own types deslaration

    //we use declare keyword to declare a type if if have to do inside the project
    //or create a separate file with extendion filename.d.ts and add declarations there
    //declare const process: any;

    //if we use the package express and wants to downlaod the type declarations the use
    //npm i @types/express
)